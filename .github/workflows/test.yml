name: Testing

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: docker://ghcr.io/artis3n/ansible_galaxy_collection-testartifact:snapshot-06f5d26a626bba513e78bea93f6872f617a59378
        with:
          api_key: ${{ secrets.GALAXY_API_KEY }}
