name: Testing

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: docker://ghcr.io/artis3n/ansible_galaxy_collection-testartifact:snapshot-79ea1722a693bdae4039ae3cfc3477bacc9db3cd
        with:
          api_key: ${{ secrets.GALAXY_API_KEY }}
