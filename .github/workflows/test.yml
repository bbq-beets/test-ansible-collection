name: Testing

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: docker://ghcr.io/artis3n/ansible_galaxy_collection-testartifact:snapshot-875ad71665acd86ff4c186dad8100b9c81fc9a95
        with:
          api_key: ${{ secrets.GALAXY_API_KEY }}
