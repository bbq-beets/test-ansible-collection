name: Testing

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: docker://ghcr.io/artis3n/ansible_galaxy_collection-testartifact:snapshot-9331436dafa69b166cc5cd380fc48f59e3a16f1e
        with:
          api_key: ${{ secrets.GALAXY_API_KEY }}
