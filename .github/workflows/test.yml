name: Testing

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: docker://ghcr.io/artis3n/ansible_galaxy_collection-testartifact:snapshot-1f3464f54f948a805ee4c166654ecbff03cdfb2b
        with:
          api_key: ${{ secrets.GALAXY_API_KEY }}
