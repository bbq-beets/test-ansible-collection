name: Testing

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: docker://ghcr.io/artis3n/ansible_galaxy_collection-testartifact:snapshot-db890ef588e38aaa8c0a5a0d6010abd1701fd86e
        with:
          api_key: ${{ secrets.GALAXY_API_KEY }}
